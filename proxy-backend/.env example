APP_HOST='0.0.0.0'
APP_PORT=1080
APP_LOG_LEVEL='info'
APP_WITH_TLS=false
APP_TLS_CRT=''
APP_TLS_KEY=''
APP_SESSION_SECRET="secret"
APP_CORS="http://localhost:1081"
APP_SESSION_COOKIE="cSessionID"

CODE_SERVER_BASE_HOST='code-server'
CODE_SERVER_BASE_PORT=8443
CODE_SERVER_WITH_USERNAME=true

AUTH_TYPE='ldap'
AUTH_LDAP_SERVER='openldap'
#AUTH_LDAP_SERVER='localhost'
AUTH_LDAP_PORT='389' # 389 for HTTP, 636 for HTTPS
AUTH_LDAP_SEARCH='dc=homelab,dc=local'
AUTH_LDAP_BIND_USER='cn=admin,dc=homelab,dc=local'
AUTH_LDAP_BIND_PASSWORD='admin'
AUTH_LDAP_UID_FIELD='uid' # sAMAccountName for AD
AUTH_LDAP_USE_TLS=false
AUTH_LDAP_ALLOW_SELF_SIGNED=false
AUTH_LDAP_TLS_CACERTFILE=''
AUTH_LDAP_SEARCH_ATTRIBUTES='memberOf'

AUTH_BACKEND='ldap'
AUTH_REGULAR_ROLES='admins'
AUTH_ADMIN_ROLES='users,customers'

JWT_ACCESS_SECRET='secret='
JWT_REFRESH_SECRET='secret'
JWT_ISSUER='csplatform-server'
JWT_AUDIENCE='csplatform-client'

REDIS_HOST='code-server-redis'
REDIS_PORT=6379
REDIS_PASSWORD='password'
REDIS_DB=1